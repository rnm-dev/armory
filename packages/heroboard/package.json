{
  "name": "@rnm-dev/armory-heroboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/mcp.ts src/hooks/configure.ts src/hooks/verify.ts --bundle --platform=node --format=esm --target=node22 --outdir=dist --outbase=src",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
